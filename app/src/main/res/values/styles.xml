<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorPrimaryDark">@color/color_primary_dark</item>
        <item name="colorAccent">@color/color_accent</item>
        <item name="android:textColorPrimary">@color/color_text_primary</item>
        <item name="android:windowBackground">@color/background_color</item>
        <item name="selectableItemBackground">?android:selectableItemBackground</item>
        <item name="selectableItemBackgroundBorderless">?android:selectableItemBackground</item>
    </style>

    <style name="ToolbarTheme" parent="@style/ThemeOverlay.AppCompat.ActionBar">
        <item name="android:textColorPrimary">@color/toolbar_text_color</item>
        <item name="android:textColorHint">@color/toolbar_text_color</item>
        <item name="actionMenuTextColor">@color/toolbar_text_color</item>
    </style>

    <!-- Base -->
    <style name="BaseProgressBar">
        <item name="android:layout_width">36dp</item>
        <item name="android:layout_height">36dp</item>
        <item name="android:indeterminate">true</item>
    </style>

    <style name="PrimaryText">
        <item name="android:textColor">@color/color_text_primary</item>
        <item name="android:textSize">@dimen/default_text_size_primary</item>
        <item name="android:maxLines">1</item>
        <item name="android:ellipsize">end</item>
    </style>

    <style name="SecondaryText">
        <item name="android:textColor">@color/color_text_secondary</item>
        <item name="android:textSize">@dimen/default_text_size_secondary</item>
        <item name="android:maxLines">1</item>
        <item name="android:ellipsize">end</item>
    </style>

    <style name="FocusText" parent="PrimaryText">
        <item name="android:textSize">@dimen/default_text_size_focus</item>
    </style>

    <style name="HighlightText" parent="PrimaryText">
        <item name="android:textColor">@color/color_primary_dark</item>
    </style>

</resources>
